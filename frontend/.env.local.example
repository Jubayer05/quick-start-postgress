# Copy to .env.local: cp .env.local.example .env.local

# Must match the backend origin (used by next.config rewrites and server-side API base URL)
BACKEND_URL=http://localhost:5000

# Optional: override API base; leave unset to use same-origin /api via Next rewrites
# NEXT_PUBLIC_API_URL=
